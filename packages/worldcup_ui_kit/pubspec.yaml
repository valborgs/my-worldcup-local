name: worldcup_ui_kit
description: Shared design system, theme and reusable widgets for My World Cup.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  worldcup_core:
    path: ../worldcup_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
