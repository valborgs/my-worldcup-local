/// feature_worldcup_share 공개 API.
///
/// 앱 셸의 라우터만 이 배럴을 쓴다. 다른 feature 패키지는 이 패키지에
/// 의존하지 않고, worldcup_core의 AppRoutes로만 전송 화면에 도달한다.
library;

export 'src/screens/nearby_worldcup_transfer_screen.dart';
