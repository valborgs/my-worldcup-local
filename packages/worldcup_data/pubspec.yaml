name: worldcup_data
description: Port implementations for My World Cup — sqflite, ImgBB, package archive, ads, remote config.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  worldcup_core:
    path: ../worldcup_core
  worldcup_domain:
    path: ../worldcup_domain
  worldcup_nearby_transfer:
    path: ../worldcup_nearby_transfer

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
