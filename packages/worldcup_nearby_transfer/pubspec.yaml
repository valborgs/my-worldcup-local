name: worldcup_nearby_transfer
description: Project-local Google Nearby Connections transport for My World Cup.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.4.1 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  plugin:
    platforms:
      android:
        package: org.comon.worldcup_nearby_transfer
        pluginClass: WorldcupNearbyTransferPlugin
      ios:
        pluginClass: WorldcupNearbyTransferPlugin
